---
import DarkMode from "@components/DarkMode/DarkMode.astro"
import "../styles/global.css"
import Nav from "@components/Nav.astro"

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/katex@0.15.2/dist/katex.min.css"
			integrity="sha384-MlJdn/WNKDGXveldHDdyRP1R4CTHr3FeuDNfhsLPYrq2t0UBkUdK2jyTnXPEK1NQ"
			crossorigin="anonymous"
		/>
		<script src="../components/DarkMode/dark-mode-init.ts"></script>
	</head>
	<body>
		<div class="z-50 fixed top-0 left-0 m-auto h-[50px] py-md w-full bg-ivory/50 dark:bg-shark-950/50 backdrop-blur-sm">
			<div class="flex mx-auto px-lg lg:px-0 max-w-screen-md">
				<Nav/>
				<div class="ml-auto">
					<DarkMode/>
				</div>
			</div>
		</div>
		<slot />
	</body>
</html>

<style is:global>
	main {
		@apply px-lg lg:px-0 max-w-screen-md;
		padding-top: calc(50px + 10px);
	}
</style>