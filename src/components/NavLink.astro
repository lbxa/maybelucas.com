---
const { href, label } = Astro.props;
const path = Astro.url.pathname;
const isActive = path === href || path.startsWith(href + "/");
---

<a
  href={href}
  class={`font-mono no-underline ${isActive ? "text-blue-700 dark:text-blue-300 transition-colors duration-200" : ""}`}
>
  {"["}<span>{label}</span>{"]"}
</a>
