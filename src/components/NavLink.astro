---
const { href, label } = Astro.props;
const path = Astro.url.pathname;
const isActive = path === href || path.startsWith(href + '/');
---

<a href={href} class={`font-mono hover:underline ${isActive ? 'underline' : ''}`}>
  {label}
</a>

